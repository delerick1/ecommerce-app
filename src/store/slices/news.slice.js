import { createSlice } from '@reduxjs/toolkit';

export const mySlice = createSlice({
    name: '',
    initialState: undefined,
    reducers: {

    }
})

export const {  mySlice} = mySlice.actions;

export default mySlice.reducer;
